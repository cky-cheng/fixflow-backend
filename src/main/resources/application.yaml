spring:
  application:
    name: fixflow-backend
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc://localhost:3306/fixflow?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 985211

server:
  port: 8080
mybatis:
  configuration:
    map-underscore-to-camel-case: true  # 下划线转驼峰

logging:
  level:
    com.kory.fixflow: debug
